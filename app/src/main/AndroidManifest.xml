<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.pandora.carlauncher">

    <!-- ========== 系统核心权限 ========== -->
    
    <!-- 悬浮窗权限 - 用于智能悬浮球和导航控件 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- 车辆相关权限 -->
    <uses-permission android:name="android.car.permission.CONTROL_CAR_CLIMATE" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_MEDIA" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_DCM" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_NAVIGATION" />
    <uses-permission android:name="android.car.permission.READ_CAR_DISPLAY_AMBIENT_LIGHT_SENSOR" />
    <uses-permission android:name="android.car.permission.READ_CAR_SENSOR_SPEED" />
    
    <!-- 网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    
    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" 
        tools:ignore="ScopedStorage" />
    
    <!-- 音频权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- 系统权限 -->
    <uses-permission android:name="android.permission.SYSTEM_UI_TUNNEL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" 
        tools:ignore="ProtectedPermissions" />
    
    <!-- 定位权限 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- 通知权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ========== 功能声明 ========== -->
    
    <!-- 车辆应用特性 -->
    <uses-feature android:name="android.hardware.type.automotive" 
        android:required="true" />
    <uses-feature android:name="android.hardware.wifi" 
        android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" 
        android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" 
        android:required="false" />
    <uses-feature android:name="android.hardware.microphone" 
        android:required="false" />

    <application
        android:name=".PandaCarApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PandaCarLauncher"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        
        <!-- 系统级应用配置 -->
        android:sharedUserId="android.uid.system"
        android:persistent="true"
        
        tools:targetApi="34">
        
        <!-- ========== 主启动器配置 ========== -->
        
        <!-- 主桌面 Activity -->
        <activity
            android:name=".ui.activity.MainLauncherActivity"
            android:exported="true"
            android:launchMode="singleInstance"
            android:stateNotNeeded="true"
            android:excludeFromRecents="true"
            android:finishOnTaskLaunch="false"
            android:theme="@style/Theme.PandaCarLauncher.Home">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.MONKEY" />
                <category android:name="android.intent.category.CAR_LAUNCHER" />
            </intent-filter>
            
            <meta-data
                android:name="android.service.quickstep.TAP_OUTSIDE_ALLOWED"
                android:value="true" />
        </activity>
        
        <!-- ========== 功能模块 Activity ========== -->
        
        <!-- 设置页面 -->
        <activity
            android:name=".modules.settings.SettingsActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:parentActivityName=".ui.activity.MainLauncherActivity">
            
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.MainLauncherActivity" />
        </activity>
        
        <!-- 应用管理 -->
        <activity
            android:name=".modules.appmanager.AppManagerActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainLauncherActivity" />
        
        <!-- 文件管理 -->
        <activity
            android:name=".modules.filemanager.FileManagerActivity"
            android:exported="false"
            android:parentActivityName=".ui.activity.MainLauncherActivity" />
        
        <!-- 媒体播放全屏 -->
        <activity
            android:name=".modules.media.MediaPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".ui.activity.MainLauncherActivity" />
        
        <!-- 导航搜索 -->
        <activity
            android:name=".modules.navigation.NavigationSearchActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize"
            android:windowSoftInputMode="stateVisible|adjustResize"
            android:parentActivityName=".ui.activity.MainLauncherActivity" />
        
        <!-- 工厂模式 -->
        <activity
            android:name=".modules.factorymode.FactoryModeActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />
        
        <!-- ========== 系统服务 ========== -->
        
        <!-- 悬浮球服务 -->
        <service
            android:name=".modules.floatingball.FloatingBallService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_window" />
        </service>
        
        <!-- 语音助手服务 -->
        <service
            android:name=".modules.voiceassistant.VoiceAssistantService"
            android:exported="false"
            android:foregroundServiceType="microphone">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="voice_assistant" />
        </service>
        
        <!-- 媒体播放服务 -->
        <service
            android:name=".modules.media.MediaPlayService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        
        <!-- 车辆连接服务 -->
        <service
            android:name=".core.service.CarConnectionService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property 
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="car_connection" />
        </service>
        
        <!-- ========== 广播接收器 ========== -->
        
        <!-- 启动完成接收器 -->
        <receiver
            android:name=".core.receiver.BootCompletedReceiver"
            android:exported="true"
            android:priority="1000">
            
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- 车辆状态接收器 -->
        <receiver
            android:name=".core.receiver.CarStatusReceiver"
            android:exported="false">
            
            <intent-filter>
                <action android:name="android.car.Car.intent.action.ACTION_CAR_DRIVING_STATE_CHANGED" />
                <action android:name="android.car.Car.intent.action.ACTION_CAR_SPEED_CHANGED" />
                <action android:name="android.car.Car.intent.action.ACTION_CAR_DISTANCE_CHANGED" />
            </intent-filter>
        </receiver>
        
        <!-- 媒体按钮接收器 -->
        <receiver
            android:name=".core.receiver.MediaButtonReceiver"
            android:exported="false">
            
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>
        
        <!-- 网络状态接收器 -->
        <receiver
            android:name=".core.receiver.NetworkStateReceiver"
            android:exported="false">
            
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.STATE_CHANGE" />
            </intent-filter>
        </receiver>
        
        <!-- 屏幕状态接收器 -->
        <receiver
            android:name=".core.receiver.ScreenStateReceiver"
            android:exported="false">
            
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_ON" />
                <action android:name="android.intent.action.SCREEN_OFF" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
        
        <!-- ========== Content Provider ========== -->
        
        <!-- 设置数据提供者 -->
        <provider
            android:name=".core.provider.SettingsProvider"
            android:authorities="com.pandora.carlauncher.settings"
            android:exported="false"
            android:singleUser="true" />
        
        <!-- ========== 权限配置 ========== -->
        
        <!-- 定义应用特定权限 -->
        <permission
            android:name="com.pandora.carlauncher.permission.CONTROL_HVAC"
            android:protectionLevel="signature|privileged" />
        
        <permission
            android:name="com.pandora.carlauncher.permission.FACTORY_MODE"
            android:protectionLevel="signature|privileged" />
        
    </application>

</manifest>
